# 本插件的核心依赖大多已包含在 AstrBot 的默认依赖中，且在插件下载安装时会自动安装插件所需的依赖，通常无需额外安装。
# 此文件主要用于明确声明依赖，以备不时之需。
apscheduler>=3.11.3,<4.0
aiofiles>=25.1.0
fastapi>=0.141.1
uvicorn>=0.52.4
tomli>=2.4.1,<3.0 ; python_version < "3.11"
